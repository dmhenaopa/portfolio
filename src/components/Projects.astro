---
import ProjectItem from "./ProjectItem.astro";
import { Projects as projects } from "../data/projects";
---

<ol class="relative ml-4">
  {
    projects.map((project) => (
      <li class="mb-15 ms-4 relative">
        <ProjectItem {...project} />
      </li>
    ))
  }
</ol>
